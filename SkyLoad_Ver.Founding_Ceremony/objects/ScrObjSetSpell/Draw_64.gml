draw_text_transformed_color(draw_x, draw_y, spell_name, scale, scale, 0, c_black,c_black,c_black,c_black,255)

if (count > 0 && count < 40){
	scale -= 0.08;
}
	
	
if(count > 50 && count < 90){
	draw_y -= 10.2;
}
	 